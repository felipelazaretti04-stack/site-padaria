# TODO - Correções no Site da Padaria v2

## Crítico / para hoje
- [ ] Adicionar `alt` em todas as imagens sem `alt` (começar por `detalhes_produto.html`).
- [ ] Corrigir todos os `<button>` sem `type`. (Adicionar `type="button"` ou `type="submit"` conforme o caso).
- [ ] Revisar todos os formulários: adicionar `label`, `id`, `name`, validação HTML5.
- [ ] Substituir autenticação client-side em `js/admin.js` por comentário explicativo e remover senha hardcoded.

## Alto / esta semana
- [ ] Adicionar `meta description` e Open Graph tags a todas as páginas.
- [ ] Corrigir links do header e menu mobile (testar navegando entre todas as páginas).
- [ ] Garantir `lang="pt-BR"` no `<html>` de todas as páginas.

## Médio
- [ ] Ajustar headings (`<h1>` único por página).
- [ ] Verificar e corrigir foco/ordem tab (acessibilidade, por ex. quando abrir modal ou menu mobile).
- [ ] Adicionar `aria-expanded` controlado por JS no botão de menu mobile.

## Baixo / optional
- [ ] Produzir `sitemap.xml` e `robots.txt` para SEO.
- [ ] Adicionar favicons e imagens OG de alta qualidade.
- [ ] Rodar validador HTML (W3C) e linter CSS/JS.
